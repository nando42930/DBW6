<!DOCTYPE html>
<html>
<%- include('../partials/head.ejs') %>
<body>
<%- include('../partials/header.ejs') %>
<div class="container">
    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <th>#</th>
            <th>Título</th>
            <th>Estado</th>
        </tr>
        </thead>
        <tbody>
        <% for (var i = 0; i < data.length; ++i) { %>
            <tr onclick="window.location.href='/ticket'">
                <td><%= i+1 %></td>
                <td><%= data[i].title %></td>
                <td>
                    <% if (data[i].state) { %>Aberto
                    <% } else { %>Fechado
                    <% } %>
                </td>
            </tr>
        <% } %>
        </tbody>
    </table>
</div>
<%- include('../partials/footer.ejs') %>
</body>
</html>